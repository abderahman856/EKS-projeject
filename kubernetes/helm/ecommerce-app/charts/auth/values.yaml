replicaCount: 1

image:
  repository: "auth-service"

service:
  type: ClusterIP
  port: 8080

database:
  name: "auth_db"

secrets:
  dbPassword: "" 
  jwtSecret: ""

resources:
  limits:
    cpu: "100m"
    memory: "128Mi"
  requests:
    cpu: "500m"
    memory: "256Mi"