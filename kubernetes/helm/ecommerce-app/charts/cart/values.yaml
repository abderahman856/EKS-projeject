replicaCount: 1

image:
  repository: cart-service
  tag: latest

service:
  type: ClusterIP
  port: 8081

resources:
  requests:
    cpu: "10m"       # Only request 1% of a core
    memory: "32Mi"   # Very small start-up memory
  limits:
    cpu: "100m"
    memory: "128Mi"
